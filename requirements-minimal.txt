# Minimal requirements for Streamlit Cloud (if default fails)
# Use this if requirements.txt times out or has compilation issues

streamlit>=1.0.0
python-dotenv>=1.0.0
langchain>=0.1.0
langchain-groq>=0.1.0
groq>=0.4.0
requests>=2.31.0
pydantic>=2.0.0
